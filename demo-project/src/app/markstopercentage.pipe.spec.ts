import { MarkstopercentagePipe } from './markstopercentage.pipe';

describe('MarkstopercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new MarkstopercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
